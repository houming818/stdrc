
## Go Setup

versions:

package=go1.20.3.linux-amd64.tar.gz
package=go1.21.6.linux-amd64.tar.gz

wget https://cdn.grepcode.cn/go/$package

rm -rf /usr/local/go && tar -C /usr/local -xzf $package
echo 'export PATH=$PATH:/usr/local/go/bin' >> ~/.bashrc

